const SECOND = 1000;
const MINUTES = SECOND * 60;
const HOURS = MINUTES * 60;
const DAYS = HOURS * 24;

const REF_RATE = 5; // units

const STARTING_BALANCE = 50;

module.exports = {
    SECOND,
    MINUTES,
    HOURS,
    DAYS,
    REF_RATE,
    STARTING_BALANCE
};